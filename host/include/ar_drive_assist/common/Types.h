#pragma once
// TODO(Part 5): Shared plain types used across subsystem boundaries (VehiclePose, DetectionFrame,
// SceneModel, ActuationRequest, config structs). FlatBuffers schemas in common/schemas/ cover the
// messages that cross the bus; this header covers the in-process value types.
//
// Filled in during Phase 3.
