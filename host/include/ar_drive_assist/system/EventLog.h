#pragma once
// TODO(Part 11.6): EventLog — every line carries a monotonic timestamp and is flushed immediately,
// not buffered. This file is the actuation-evidence trail replayed in the report and viva, so it
// must survive a crash, not just a clean shutdown.
//
// Filled in during Phase 3.
