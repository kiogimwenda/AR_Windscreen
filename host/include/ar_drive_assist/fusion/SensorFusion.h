#pragma once
// TODO(Part 8.2): SensorFusion — 5-state EKF [x, y, heading, speed, yawRate]. IMU-driven predict()
// at 100 Hz, corrected by updateGps() and updateObd(). LiDAR-odometry correction is deliberately
// NOT added for v1 (Part 8.2).
//
// Filled in during Phase 7.
