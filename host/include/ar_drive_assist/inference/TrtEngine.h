#pragma once
// TODO(Part 7.4): TrtEngine — thin wrapper over one TensorRT engine: load(), async infer() on its
// own CUDA stream. One instance per model so the three models can overlap rather than serialize.
//
// Filled in during Phase 5.
