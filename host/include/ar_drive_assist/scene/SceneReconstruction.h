#pragma once
// TODO(Part 8.3): SceneReconstruction — associates each 2D detection box with the nearest LiDAR
// cluster by projecting the cluster centroid into image space using lidar_camera_extrinsics.yaml,
// giving every detected object a real-world distance and velocity rather than just a pixel box.
//
// Filled in during Phase 7.
