#pragma once
// TODO(Part 8.1): GroundPlaneModel — the fitted road surface, PUBLISHED rather than discarded.
// Plane coefficients plus the actual measured near-field ground points and the range beyond which
// no measured surface exists. RoadSurfaceProjector (Part 11.4) depends on this being real, current
// geometry.
//
// Filled in during Phase 6.
