#pragma once
// TODO(Part 10.3): ArRenderer — composites bounding boxes, lane overlay, navigation overlay and
// speed/warnings onto the camera frame in plain OpenCV, in the camera's own image-space pixel
// coordinates (video-see-through, Part 0 — no projector optical model). Far-field flat-ground nav
// points are drawn differently from near-field measured-surface points, as an honest cue that
// precision degrades with distance.
//
// Filled in during Phase 11.
