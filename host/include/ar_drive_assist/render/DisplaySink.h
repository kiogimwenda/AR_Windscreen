#pragma once
// TODO(Part 10.1): DisplaySink — the abstraction separating rendering logic from the physical
// output device, so a future ProjectorSink is a clean swap with no change to perception or decision
// code. Kept exactly as specified even though only WindowedSink is implemented now.
//
// Filled in during Phase 11.
