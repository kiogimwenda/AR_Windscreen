#pragma once
// TODO(Part 11.2): NavigationEngine — OSRM routing (MLD algorithm) against an offline OSM extract.
// routeTo() returns full road-network geometry, not just turn text, because MapMatcher and
// RoadSurfaceProjector need that geometry. A primary project objective, not a stretch feature.
//
// Filled in during Phase 8.
